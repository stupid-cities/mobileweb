extends layout
block content
	h1 Upload
		form(method='post' enctype='multipart/form-data')
			input(type='file' name='fileupload')
			input(type='submit' value='Upload')
	h1 Files
	ul
		each l in links 
			li
				a(href=l.link)= l.title